class Solution:
    def longestCommonPrefix(self, strs: List[str]) -> str:
        l=list(zip(*strs))
        p=""
        for i in l:
            if len(set(i))==1:
                prefix +=i[0]:
            else:
                break
        print(p) 
